/**
 * ========= index.ts rules =========
 * Only use index.ts for exports
 * Only expose public API in index.ts
 * Signals to maintainers that all the other components are for the internal API
 */
export * from './Button';
